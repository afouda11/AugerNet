# AugerNet

This code has been designed so that users can both generate rapid CEBE (carbon 1s only present release) predictions and
retrain the model with various combinations of data splitting, features specifications and hyperparamters

The
next release of the 

After succesful installation of the package, in your chosen working directory run:

python -m augernet --config /path/to/config.yml

All options and paramters are defined in a .yml congfig file




